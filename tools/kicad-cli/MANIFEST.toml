# kicad-cli versions staged in the independent binary cache.
#
# Binaries themselves live under KICAD_CLI_CACHE_ROOT, NOT in this
# repo (each version is several hundred megabytes). This manifest is the
# authoritative pointer: tests/kicad_cli_resolver.py prefers builds in
# the order they are listed here over any other staged build found by
# mtime. The cache can contain stale experimental builds that must not
# be picked up just because their mtime is newer.
#
# To add a new staged version, build kicad-cli from a clean checkout,
# copy the resulting release tree to
#   <cache-root>/<short-hash>/{bin,share,lib,etc}
# and append a [[versions]] block here.

cache_layout = "<short-hash>/{bin,share,lib,etc}"
license = "GPL-2.0-or-later"

[[versions]]
short_hash     = "f11d3da6771a"
kicad_commit   = "f11d3da6771a56ac3df742bbfc069d20e2c24cde"
kicad_version  = "10.99.0"
build_type     = "RelWithDebInfo"
platform       = "win32-x64"
staged_on      = "2026-05-12"
sch_format_max = 20260410
recorder       = true
notes = """
Active schematic SVG/IR oracle build (RECORDER_PLOTTER patches +
eeschema KICAD_RECORDER_OUTPUT hook). Schematic-only staging: no
_pcbnew.dll, so the resolver's pcb_svg capability probe skips it.
"""

[[versions]]
short_hash     = "76f8839fd232"
kicad_commit   = "76f8839fd232594f81180f644e3370c390025144"
kicad_version  = "10.99.0"
build_type     = "RelWithDebInfo"
platform       = "win32-x64"
staged_on      = "2026-05-09"
sch_format_max = 20260410
notes = """
Active PCB SVG oracle build. Self-contained release tree (bin + share)
built from canonical KiCad source. Includes upstream commit 934a5bac34
(FROM_CHARS_FLAGS): scientific notation in .kicad_pcb round-trips
correctly, unlike the kicad-win-builder stagings (99c64afbf83f /
00911f90fd99), which are stale and must not be used as oracles. The
win-builder builds also compute SVG page origin with half-stroke
inflation, producing a constant 0.025 mm draw-item shift vs the
reference output used by L3_016.
"""

[[versions]]
short_hash     = "b70f2b514aa2-irbbox"
kicad_commit   = "b70f2b514aa2c83c0726590a8df5c080fbc614c0"
kicad_version  = "10.99.0"
build_type     = "RelWithDebInfo"
platform       = "win32-x64"
staged_on      = "2026-06-19"
sch_format_max = 20260410
pcb_format_max = 20260410
recorder       = true
bbox_oracle    = true
source_dirty   = true
r2_bucket      = "wn-build-deps"
r2_prefix      = "deps/v1/kicad-monkey/kicad-cli/10.99.0-b70f2b514aa2-irbbox/native/windows-x64/kicad-cli-10.99.0-b70f2b514aa2-irbbox-native-windows-x64-relwithdebinfo-recipe-64a838113c327636/"
r2_archive     = "kicad-cli-bundle.zip"
r2_sha256      = "4e469a92deeb140e3b172689b868d1fa0b29498e627d3d1489e443f510e9c95a"
notes = """
Bbox oracle build from the local ir_recorder branch at upstream commit
b70f2b514aa2 with Wavenumber local patches. Provides
`kicad-cli pcb export bbox` emitting `kicad.pcb_bbox.v1`. Appended after
the active PCB SVG oracle so existing pcb_svg resolver behavior continues
to prefer 76f8839fd232.
"""
