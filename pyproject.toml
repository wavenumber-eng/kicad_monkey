[project]
name = "kicad-monkey"
version = "2026.8.17"
description = "Core KiCad parser, round-trip, and 2D rendering tooling"
readme = "README.md"
requires-python = ">=3.11,<3.15"
license = { text = "MIT" }
authors = [
    { name = "Wavenumber LLC" },
]
dependencies = [
    "colorama",
    "freetype-py",
    "numpy",
    "shapely",
    "trimesh",
    "uharfbuzz",
    "zstandard",
    "msgspec>=0.21.1",
]

[project.optional-dependencies]
test = [
    "brotli>=1.1.0",
    "fonttools>=4.53.0",
    "jsonschema>=4.22.0",
    "pytest>=8.0.0",
    "pytest-json-report",
    "tomli-w>=1.2.0",
    "wn-rack>=1.1.0",
    "pyright>=1.1.390",
    "ruff>=0.8.0",
    "twine>=5.0.0",
    "build>=1.2.0",
    "wn-dev-std>=2026.8.12; python_version >= '3.12'",
]

[dependency-groups]
dev = [
    "brotli>=1.1.0",
    "fonttools>=4.53.0",
    "jsonschema>=4.22.0",
    "pytest>=8.0.0",
    "pytest-json-report",
    "tomli-w>=1.2.0",
    "wn-rack>=1.1.0",
    "pyright>=1.1.390",
    "ruff>=0.8.0",
    "twine>=5.0.0",
    "build>=1.2.0",
    "wn-dev-std>=2026.8.12; python_version >= '3.12'",
]

[project.urls]
Homepage = "https://github.com/wavenumber-eng/kicad_monkey"
Repository = "https://github.com/wavenumber-eng/kicad_monkey"
Issues = "https://github.com/wavenumber-eng/kicad_monkey/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.wn_dev_std]
standard_version = "2026.8.12"
enabled_scopes = ["repo", "language", "ci", "docs.design", "docs.links", "docs.plans"]
profile = "python-package"
distribution = "pypi"
languages = ["python", "rust"]
strict = true

[tool.wn_dev_std.pr_hygiene]
enabled = true

[tool.wn_dev_std.rust]
source_root = "src/rs"

[tool.wn_dev_std.rust.hygiene]
config = "rust-hygiene.toml"

[tool.wn_dev_std.documentation.standard_docs]
rust = "docs/design/rust-standard.html"

[tool.uv]
package = true

[tool.uv.workspace]
members = ["packages/kicad_cruncher"]

[tool.hatch.build.targets.wheel]
packages = ["src/py/kicad_monkey"]

[tool.hatch.build.targets.wheel.hooks.custom]
path = "hatch_build.py"

[tool.hatch.build.targets.sdist]
include = [
    "Cargo.toml",
    "Cargo.lock",
    "hatch_build.py",
    "assets/**",
    "src/rs/**",
    "src/py/**",
    "tests/**",
    "docs/**",
    "scripts/generate_kicad_stroke_font_data.py",
    "tools/generate_kicad_stroke_webfont.py",
    "tools/package_kicad_stroke_webfont_assets.py",
    "README.md",
    "CONTRIBUTING.md",
    "CHANGELOG.md",
    "LICENSE",
    "AGENTS.md",
    "pyproject.toml",
]
exclude = [
    "packages/**",
    "docs/plans/**",
    "docs/research/**",
    "rack_results/**",
    "tests/corpus/**",
    "tests/cross_package/**",
    "tests/**/output/**",
    "**/__pycache__/**",
]

[tool.hatch.version]
path = "src/py/kicad_monkey/_version.py"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src/py", ".."]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
markers = [
    "slow: long-running test lanes",
]
