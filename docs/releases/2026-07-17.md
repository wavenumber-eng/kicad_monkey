# 2026-07-17 Performance Optimization Candidate

Release version: pending external review and release authorization

This candidate improves large-board PCB parse and projection performance while
preserving parser, projection, source-span, and net-reference behavior. It is
not a published release note until the external review gate and explicit
release authorization are complete.

## Notes

- Reworks S-expression lexer and projection scanner hot paths in pure Python,
  without adding native dependencies or changing the public token/span
  contracts.
- Adds source line-column indexing for nested PCB projection source-span
  rebasing while preserving exact offsets, source text, and reparsed
  S-expression behavior.
- Caches direct child spans per parent during projection hydration so pad and
  model reference scans do not repeatedly walk the same footprint source text.
- Reuses PCB net lookup maps in full parse and projection paths while
  preserving KiCad v10 name-only net references when no top-level net table is
  present.
- Records final three-round performance evidence in
  `docs/plans/logs/2026-07-17-performance-signoff-summary.md`.
- Treats public PRs #18 and #19 only as research inputs; the implementation is
  independently rewritten on the performance branch.

## Validation

- `uv run --extra test python tests/rack.py run L0_foundation`
- `uv run --extra test python tests/rack.py run L99_signoff`
- `uv run --extra test python -m pytest tests/L1_parsing/test_L1_020_pcb_projection_corpus.py`
- `uv run dev-std audit . --format json`
