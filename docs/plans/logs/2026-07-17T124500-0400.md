+++
type = "plan_log"
id = "kicad-monkey-project-api-projection-render-design-2026-07-17T124500-0400"
plan_id = "kicad-monkey-project-api-projection-render-design"
step_id = "public-issue-response"
created = "2026-07-17T12:45:00-04:00"
+++

# Log: public-issue-response guide revision

Addressed strict documentation review `2026-07-17T123500-0400` before the
workflow guide is cited publicly.

## Fixes

- Revised `docs/guides/project-workflows.html` so projection is described as a
  memory and narrow-scan tool, not a general wall-clock replacement for full
  parsing.
- Added measured cost anchors from the fresh WREN baseline:
  full parse about 26.7s, route projection about 20.0s, common-family
  projection about 32.0s.
- Split netlist and design JSON rows so the lazy PCB-load distinction is
  explicit.
- Removed `nets` from the projection recommendation and added the KiCad v10
  no-top-level-net-table caveat for `projection.nets()`.
- Fixed `build/` creation in README and guide examples before
  `design.save_json("build/design.json")`.
- Normalized route-count examples to use `len(...)` because projection getters
  return lists.
- Removed native-acceleration roadmap wording from the user-facing guide and
  kept it in the requirements deferred-work section.
- Made the guide canonical by shrinking README and API design docs to pointers
  for read-path tradeoffs.

No GitHub issue comment was posted. `public-issue-response` remains active and
`ec-public-issue-response-authorized` remains pending.
