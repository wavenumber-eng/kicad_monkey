+++
type = "plan_log"
id = "kicad-monkey-project-api-projection-render-design-2026-07-17T140000-0400"
plan_id = "kicad-monkey-project-api-projection-render-design"
step_id = "external-review"
created = "2026-07-17T14:00:00-04:00"
+++

# Log: external-review (re-review of 2210e90)

Independent re-review of the workflow guide revision (`2210e90`) against the
strict review findings in `2026-07-17T123500-0400`.

## Verdict

**Approved with one residual nit.** All three blocking findings and all five
tightening items from the strict review are correctly addressed. Validation
independently reproduced: `dev-std audit` passed, L99 21 passed.

## Findings resolution verified

1. Projection overpromise: fixed. Table split into a narrow-scan row and a
   "measure both" row for several common/nested families; prose now says
   projection "can reduce wall-clock time and memory for narrow family scans"
   and warns broad/nested requests can match or exceed full-parse cost.
2. `nets` caveat: fixed. `nets` removed from the projection recommendation; a
   dedicated paragraph explains v10 boards can omit the top-level net table so
   `projection.nets()` can legitimately be empty.
3. Cost anchors: fixed. WREN 59.6 MB anchors added (full ~26.7s, routes
   ~20.0s, common ~32.0s), correctly hedged as branch-current,
   order-of-magnitude, not release gates.
4. `save_json` mkdir: fixed in both guide and README examples.
5. Netlist vs design JSON lazy-load split: fixed in the decision table.
6. `len(...)` idiom normalized in README.
7. Native-acceleration roadmap removed from the guide, retained in the
   requirements deferred-work section.
8. Canonicalization: README bullets and the three API design docs shrunk to
   pointers at the guide; relative links (`../../guides/...`) resolve.

## Residual nit (non-blocking)

The "Large PCB Inventory Workflows" example still requests six families
(footprints, pads, zones, segments, vias, model references) — the broad
pattern the guide's own table now says to measure-both. The qualifying prose
directly below mitigates this, but trimming the example to a narrow scan
(routes plus model references), or labeling it as a broad-inventory example
subject to the measure-both caveat, would remove the last internal tension.
The section's opening sentence ("Use KiCadPcbProjection for large-board
inventories") could likewise say "narrow large-board inventories".

## Scope

Commit `2210e90` only. No push, PR, tag, release, PyPI upload, public issue
response, or downstream `kicad_cruncher` work was performed or authorized by
this review.
