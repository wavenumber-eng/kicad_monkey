+++
type = "plan_log"
id = "kicad-monkey-project-api-projection-render-design-2026-07-17T115500-0400"
plan_id = "kicad-monkey-project-api-projection-render-design"
step_id = "external-review"
created = "2026-07-17T11:55:00-04:00"
+++

# Log: external-review

Independent review of the api-design-decision and durable-docs implementation
(commits `101ebaa`, `cfb0ad7`) by the same second agent that reviewed the
performance sweep and the research handoff.

## Verdict

**Approved.** The documentation-only implementation matches the recorded
decision, all prior review corrections were incorporated, the durable docs are
accurate against live code, and validation was independently reproduced. No
blocking findings.

## Verified

- **Decision completeness.** The decision log (T112000) records an
  accept/reject/defer rationale for all six listed candidates, including the
  previously-uncovered #2 (inventory helpers: deferred) and #4 (render cache:
  deferred). The harness-sensitivity caveat on the raw-scan number is folded
  into candidate 6. All three corrections from the research review (T111500)
  are addressed.
- **PERF-REQ-008 evidence restored.** The "External Review Evidence" section in
  `docs/requirements/2026-07-17-performance-optimization-requirements.html`
  cites the fresh Jumperless decomposition (8,350,129 matches, 5,236,594
  tokens, tokens() 9.541s, parse_sexp 11.654s) and the committed independent
  review log. This closes the evidence gap created when the third perf-sweep
  review record was destroyed at closeout.
- **README examples verified against live corpus.** The `KiCadPcbProjection`
  example runs against JumperlessV5r7.kicad_pcb: 1,025 model references with
  working `.reference`/`.path` attributes; 16,619 route objects. The
  `iter_kicad_objects_from_file(..., SchSymbol)` example runs against
  JumperlessV5r7.kicad_sch (452 symbols), JustTheLEDs.kicad_sch (543), and
  4-ch-backplane sub-sheets (ETHERNET 12, DEBUG 16), with working
  `.reference`/`.value`. A zero result on the 4-ch-backplane top sheet is
  legitimate: it is a hierarchical cover sheet with no placed symbols. All
  README imports resolve from the top-level `kicad_monkey` package.
- **Design docs match code.** Cost Model sections in pcb-api, project-design-api,
  and foundation-api restate previously code-verified facts: full-model render
  paths, `layers=` filtering after full IR construction
  (`kicad_pcb_ir_svg.py:395/:427/:454`), lazy PCB load in `KiCadDesign`
  (`kicad_design.py:285-291`), targeted reader as the schematic narrow-read
  path with `KiCadSchematicProjection` explicitly deferred.
- **Plan bookkeeping.** `file-level-ir-svg-api-implementation` is closed by
  documented rejection per the plan's stated convention. Step statuses and exit
  criteria are consistent; `external-review` is the active gate.
- **Validation reproduced.** `uv run dev-std audit . --format json` passes;
  `tests/L99_signoff` passes (21 passed) in this reviewer's run.

## Non-blocking notes

1. Three steps marked done have no dedicated step log
   (`native-acceleration-followup-decision`, `design-doc-intent-audit`,
   `test-runtime-impact-audit`); their substance is distributed across
   T112000/T113000/T113500. Acceptable since the audit passes, but future
   plans should keep one log per audit-bearing step for traceability.
2. This log lives under gitignored `docs/plans/`; it must be force-added to be
   preserved.

## Scope

Commits `101ebaa` and `cfb0ad7` only. No push, PR, tag, release, PyPI upload,
public issue response, or downstream `kicad_cruncher` work was performed or
authorized by this review.
