# KiCad Cruncher 2026-06-19

Release version: `2026.6.19`

Date: 2026-06-19

## Highlights

- Updates the controlled `kicad-monkey` runtime dependency to
  `kicad-monkey==2026.6.19`.
- Carries the `kicad-monkey` PCB graphical bounds fix and KiCad-backed bbox
  oracle coverage into the public CLI install set.
- Confirms `kicad-cruncher` uses the IR/SVG bounds helper path for PCB SVG
  canvas bounds and does not depend on the removed direct PCB SVG renderer.
- Keeps `wn-geometer==2026.6.10`.

## Compatibility

Existing design-review, PCB SVG, Megamaid, library extraction, health, plugin,
and daemon command invocation contracts are unchanged. This is a controlled
dependency alignment release.
