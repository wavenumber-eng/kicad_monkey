# KiCad Cruncher 2026-06-13

Release version: `2026.6.13`

## Highlights

- Updates the controlled `kicad-monkey` runtime dependency to
  `kicad-monkey==2026.6.13`.
- Picks up the KiCad-parity hierarchical netlist fixes used by
  `design`, `design-review`, and the `dr` alias.
- Adds the sanitized 4-channel backplane project to the regular public
  workflow corpus so the release suite covers the replicated hierarchy that
  exposed the netlist mismatch.
- Refreshes public workflow fixture counts for component materialization that
  now follows KiCad's exported netlist more closely.
- Keeps `wn-geometer==2026.6.10`.

## Compatibility

This release does not change the public CLI shape. Existing `design`,
`design-review`, `dr`, BOM/PnP/JLC, PCB SVG, and KiCad helper commands keep the
same invocation contract.
